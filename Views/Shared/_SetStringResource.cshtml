@model string

<script>
    // The WYSIWYG editor functionality is now handled by wysiwyg-editor.js
    // This script is kept for compatibility but most logic has been moved
    (function ($) {
        "use strict";

        // Store the content property for backward compatibility
        let elementContentProperty = '@Model';

        // No longer need to manually bind events here as wysiwyg-editor.js handles it
        // Keep this file for any legacy compatibility needs

    })(window.$);
</script>
